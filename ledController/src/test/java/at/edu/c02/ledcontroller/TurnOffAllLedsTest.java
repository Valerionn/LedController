package at.edu.c02.ledcontroller;

import org.json.JSONObject;
import org.junit.Test;

import java.io.IOException;

import static org.junit.Assert.assertTrue;

public class TurnOffAllLedsTest {
    /**
     * This test is just here to check if tests get executed. Feel free to delete it when adding your own tests.
     * Take a look at the stack calculator tests again if you are unsure where to start.
     */
 /*   @Test
    public void dummyTest() {
        ApiService mock = Mockito.mock(ApiService.class);
        LedControllerImpl ledController = new LedControllerImpl(mock);
        try {
            ledController.turnOffAllLeds();
        } catch (IOException e) {
            throw new RuntimeException(e);
        }
    }*/

    //Test hat nicht funktioniert und wurde wie besprochen auskommentiert
}
